function solve(arr, delimiter) {

    let output = arr.join(delimiter);
    console.log(output);

    
}

solve(['One', 
'Two', 
'Three', 
'Four', 
'Five'], 
'-');

solve(['How about no?', 
'I',
'will', 
'not', 
'do', 
'it!'], 
'_');